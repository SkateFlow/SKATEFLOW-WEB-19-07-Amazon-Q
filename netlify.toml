[build]
  command = "npm install --legacy-peer-deps && npm run build"
  publish = "build"
  environment = { NODE_ENV = "development" }

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
